# 2026-07-09 — SEO Brain runs

## run41 — Rates redesign QC verification + fast-path re-anchor (RESULT: MEASURED/verified, re-anchored)

**Trigger:** The 30-run frozen-IDLE streak (runs 11–40) BROKE. `git rev-parse HEAD:public`
returned `77a6330`, not the stored `09a40ce` — Jesse shipped commit `0c61b1e`
("Rates page redesign: live-terminal hero (ticker tape, countdown, chart crosshair, rate
lab) + sitewide em-dash cleanup"), already live on origin/main. Per the SKILL 1/4 fast-path,
a DIFFERENT public/ hash forces the full QC battery — this is the first time the
"DIFFERENT → full-QC → re-anchor" branch has actually fired (runs 11–40 only ever hit the
cheap "EQUAL → IDLE" branch).

**Full QC battery on the new tree `77a6330` — CLEAN:**
- 153 HTML files, **380/380 JSON-LD blocks valid** (same count as run07's clean baseline).
- **0 broken `/img/*` refs; 0 broken og:image/twitter:image refs.**
- Canonical check: only hits are `404.html` + `offline.html` — utility pages that correctly
  omit `rel=canonical`. Known-acceptable, NOT a regression (codified in SKILLS v12).
- **Human-voice gate (hard rule):** `0` em-dashes (`—`) used as punctuation in visible prose
  after stripping HTML comments + JS placeholder `>—<` spans. The commit's "sitewide em-dash
  cleanup" is verified. Remaining `—` in source are all HTML comments (`<!-- NAV — DESKTOP -->`)
  and live-rate-calculator placeholder spans, neither of which renders as copy. En-dashes (`–`)
  in visible prose are all numeric/currency/grade ranges (`$10K–$30K`, `K–5`, `$1,500 – $3,500`)
  — standard typography, acceptable, and all pre-existing (guide/insights/neighborhoods), not
  introduced by the redesign.
- **Rates page compliance (the redesigned page):** 0 `aggregateRating`, **0 `Review` nodes**,
  clean schema set (RealEstateAgent, Organization, Service, Offer, Article, BreadcrumbList,
  City service-areas). The only visible rating claim is `5.0★ Google` — truthful per verified
  facts and **un-inflated** (no fabricated review count). No banned patterns.

**Action taken:** Re-anchored `state/last-qc.json` from `09a40ce` → `77a6330` so future idle
hours return to the near-free "EQUAL → IDLE" fast-path against the new validated-clean tree.
No production change from the Brain — the redesign already shipped clean; my job was to verify
it, which it did. seo-brain/ bookkeeping only.

**MEASURE:** still DARK. Newest harvest `gsc-harvest-2026-06-01.json` is 38 days stale;
`launchctl list` shows only `net.jessetek.seo-brain` + `net.jessetek.hourly-autopush` — the
GSC harvester (`weekly-rank-watch`) is still absent. E5 (owner=jesse, priority 0) unchanged.
No new GSC data → cannot MEASURE deltas.

**Knowledge:** best-practices-2026.md dated Jun 11 = 28 days old (<30) → no research needed.

**What I did NOT do & why:** No public/ edit — the site is QC-clean and no whitelisted SAFE
FIX is genuinely pending. Did not "fix" the 404/offline canonical hits (they're correct as-is).
Did not touch the redesign (already live, verified compliant). Did not re-raise E5/E6 (both
owner=jesse, already logged). Did not push to production (no on-page change).

**Learned:** The fast-path's re-anchor branch, dormant for 30 runs, executed correctly on its
first real hash-change event — proof the v9–v11 machinery compounds instead of churns. The
QC canonical check has a stable 2-file false-positive floor (404 + offline); codifying it
(v12) prevents a future run from mistaking it for a regression and burning a cycle.

RESULT: MEASURED (verified Rates redesign QC-clean; re-anchored last-qc.json 09a40ce→77a6330)
