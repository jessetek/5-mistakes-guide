# 2026-07-22 — seo-brain runs

## run182 — IDLE (fourth frozen re-verify after run178's C14 ship; first run of new calendar day)
Single-probe byte-identical to run179/180/181:
- tree clean
- HEAD:public `bee60a73` == recorded anchor (v15 anchor-rotation guard holding, zero false-positive across 4 re-verifies)
- rank-history.json mtime unmoved (07-20 07:31)
- gsc-harvest newest still 2026-06-01 (E5 aggregate harvester dark, 51d stale, owner=jesse)
- knowledge best-practices-2026.md 07-13 fresh (9d, <30d)

Four tiers no-op:
1. **SAFE FIX** — none. v15 latent-sweep on cooldown: 4 runs off run178's ship, << 8-run re-arm (~run186). C15 em-dash cleanup queued but outside SAFE-FIX whitelist (copy-voice pass, not structural QC).
2. **RESEARCH** — knowledge fresh.
3. **MEASURE** — byte-identical all-zero pull => dark per v14.8 (don't re-MEASURE a reading identical to the last recorded one).
4. **IDLE**.

Did NOT: run a latent-defect sweep (cooldown), touch C15 (whitelist), or re-MEASURE (byte-identical). No EVOLVE churn — v16 holding, no new failure mode observed.

RESULT: IDLE

## run183 — IDLE (fifth frozen re-verify after run178's C14 ship)
Single-probe byte-identical to run179/180/181/182:
- tree clean
- HEAD:public `bee60a73` == recorded anchor (v15 anchor-rotation guard holding, zero false-positive across 5 re-verifies)
- rank-history.json mtime unmoved (07-20 07:31)
- gsc-harvest newest still 2026-06-01 (E5 aggregate harvester dark, 51d stale, owner=jesse)
- knowledge best-practices-2026.md 07-13 fresh (9d, <30d)

Four tiers no-op:
1. **SAFE FIX** — none. v15 latent-sweep on cooldown: 5 runs off run178's ship, << 8-run re-arm (~run186). C15 em-dash cleanup queued but outside SAFE-FIX whitelist (copy-voice pass, not structural QC).
2. **RESEARCH** — knowledge fresh.
3. **MEASURE** — byte-identical all-zero pull => dark per v14.8.
4. **IDLE**.

Did NOT: run a latent-defect sweep (cooldown), touch C15 (whitelist), or re-MEASURE (byte-identical). No EVOLVE churn — v16 holding, no new failure mode observed.

RESULT: IDLE

## run184 — IDLE (sixth frozen re-verify after run178's C14 ship)
Single-probe byte-identical to run179–183:
- tree clean
- HEAD:public `bee60a73` == recorded anchor (v15 anchor-rotation guard holding, zero false-positive across 6 re-verifies)
- rank-history.json mtime unmoved (07-20 07:31)
- gsc-harvest newest still 2026-06-01 (E5 aggregate harvester dark, 51d stale, owner=jesse)
- knowledge best-practices-2026.md 07-13 fresh (9d, <30d)

Four tiers no-op:
1. **SAFE FIX** — none. v15 latent-sweep on cooldown: 6 runs off run178's ship, < 8-run re-arm (~run186 = 2 runs out). C15 em-dash cleanup queued but outside SAFE-FIX whitelist (copy-voice pass, not structural QC).
2. **RESEARCH** — knowledge fresh.
3. **MEASURE** — byte-identical all-zero pull => dark per v14.8.
4. **IDLE**.

Did NOT: run a latent-defect sweep (cooldown, 2 runs early), touch C15 (whitelist), or re-MEASURE (byte-identical). No EVOLVE churn — v16 holding, no new failure mode observed.

RESULT: IDLE

## run185 — IDLE (seventh frozen re-verify after run178's C14 ship)
Single-probe byte-identical to run179–184:
- tree clean
- HEAD:public `bee60a73` == recorded anchor (v15 anchor-rotation guard holding, zero false-positive across 7 re-verifies)
- rank-history.json mtime unmoved (07-20 07:31)
- gsc-harvest newest still 2026-06-01 (E5 aggregate harvester dark, 52d stale, owner=jesse)
- knowledge best-practices-2026.md 07-13 fresh (9d, <30d)

Four tiers no-op:
1. **SAFE FIX** — none. v15 latent-sweep on cooldown: 7 runs off run178's ship, < 8-run re-arm (~run186 = NEXT run, so run186 may run a full latent-defect sweep instead of a single-probe). C15 em-dash cleanup queued but outside SAFE-FIX whitelist (copy-voice pass, not structural QC).
2. **RESEARCH** — knowledge fresh.
3. **MEASURE** — byte-identical all-zero pull => dark per v14.8.
4. **IDLE**.

Did NOT: run a latent-defect sweep (cooldown, re-arms next run), touch C15 (whitelist), or re-MEASURE (byte-identical). No EVOLVE churn — v16 holding, no new failure mode observed.

RESULT: IDLE

## run186 — IDLE (v15 latent-defect sweep RE-ARMED; full sweep run, structural layer CLEAN)
Frozen-state probes unchanged: tree clean, HEAD:public `bee60a73` == anchor (v15 guard, 8th clean re-verify), rank-history mtime unmoved 07-20 07:31, gsc-harvest 2026-06-01 dark (E5, 52d, owner=jesse), knowledge 07-13 fresh (9d).

**8 idle runs off run178's ship => latent-sweep re-armed (per v15 cooldown).** Ran a full latent-defect QC pass over committed `public/` instead of a single-probe. Unlike the two prior re-arms (run170 future-lastmod, run178 malformed-XML), this sweep found **NO structural defect** — the earlier sitemap-generator artifacts are fixed and holding:
- xmllint: sitemap.xml / sitemap-index.xml / sitemap-images.xml / insights/feed.xml all well-formed (bare-& would fail this; all clean)
- 0 future `<lastmod>` (run170's C13 fix holding), 151 `<loc>` intact
- 0 `aggregateRating`, 0 "100+ reviews", 0 Review-with-publisher nodes (S1/S3 holding)
- 380/380 JSON-LD blocks across 154 HTML files parse valid
- 299 og:image/twitter:image refs → 0 missing local files
- Internal hrefs: all resolve. The 149 `-> /` "broken" and 1 `sms:` flagged by my resolver are FALSE POSITIVES — `/` is served via `vercel.json` rewrite `/ → /home` (cleanUrls; homepage is `public/home.html`, not index.html), and `sms:` is a valid URI scheme. Verified against vercel.json.
- Canonicals: 151/151 `jessetek.net`, 0 www. (Live apex still 307→www = known J5, owner=jesse, unchanged.)

**C15 RE-SCOPED with evidence (was framed as a 53-dash sitemap-only copy edit).** Investigated whether the sitemap-images.xml dashes should be hand-fixed. Two findings kill the one-file autonomous fix:
1. **It's site-wide, not sitemap-only:** 296 em/en-dash occurrences across **140 of 154** HTML pages (home.html alone has 15). The 53 in sitemap-images.xml are a small derived slice of a much larger content-voice debt.
2. **sitemap-images.xml is a generated, DIVERGENT artifact:** its title renders `Jesse Oñate · Realtor — Downey & Surrounding Cities` while the live `home.html` `<title>`/og:title use `|`/`:` separators (no dashes). So the sitemap caption is NOT mirrored from page OG — editing the derived file alone would desync it AND leave the real on-page violation untouched (wrong layer).

Rewriting em-dashes into natural voice is a copy judgment (number ranges `$3,000–$8,000` → "to"; clause dashes → commas/periods; brand separators → `|`), context-dependent per string — a blind sed would corrupt copy. "Rewrite marketing copy" is NOT in the SAFE-FIX whitelist, and visible-copy changes are Jesse's call (cf. C11). => C15 correctly stays **owner=jesse / dedicated human-voice skill pass with review** (grep QC gate), NOT an autonomous bulk edit. Reframed the ledger item accordingly.

Four tiers: SAFE FIX — sweep found nothing whitelisted/shippable (structural clean; the one real content defect is out-of-whitelist site-wide voice debt). RESEARCH — knowledge fresh. MEASURE — byte-identical all-zero => dark (v14.8). => IDLE.

EVOLVE v17 (below): falsify the v16 "sweep finds a ship every re-arm" claim; record that the structural surface is now well-covered/holding and a clean sweep is SUCCESS, not a miss.

Did NOT: hand-edit sitemap-images.xml (derived + wrong layer), bulk-rewrite the 296 site-wide dashes (out-of-whitelist voice pass, owner=jesse), or ship anything (structural layer already clean).

RESULT: IDLE

## run187 — IDLE (frozen re-verify; latent-sweep back on cooldown after run186's clean sweep)
Frozen-state probes unchanged: tree clean, `git rev-parse HEAD:public` = `bee60a73` == anchor (v15 guard, 9th clean re-verify), rank-history mtime unmoved 07-20 07:31, gsc-harvest 2026-06-01 dark (E5, ~51d, owner=jesse), knowledge best-practices-2026.md 07-13 fresh (9d, <30d).

Four tiers no-op:
1. **SAFE FIX** — none. v15 latent-sweep on cooldown: 1 run off run186's full clean sweep, << 8-run re-arm (~run194). C15 site-wide em-dash cleanup queued but out-of-whitelist (copy-voice, owner=jesse).
2. **RESEARCH** — knowledge fresh.
3. **MEASURE** — byte-identical all-zero pull => dark per v14.8.
4. **IDLE**.

**Near-miss this run (→ EVOLVE v18):** first probed the anchor with `git log -1 --format=%H -- public/`, which returned `5bbc6e61` (the last COMMIT touching public/ = run178's C14 ship) and momentarily read as "anchor rotated." It did not — the v15 anchor is a **tree** hash (`git rev-parse HEAD:public` = content of public/), not a commit hash; the two are different objects and only the tree hash is content-stable across seo-brain-only commits. Confirmed `bee60a73` still current. Logged the distinction as a guard so no future run false-alarms (or, worse, "corrects" the anchor to a commit hash).

Did NOT: run a latent sweep (cooldown, 7 runs early), touch C15 (out-of-whitelist), or re-MEASURE (byte-identical). No structural change — v17 holding.

RESULT: IDLE

## run188 — IDLE (frozen re-verify; 10th clean anchor check, latent-sweep on cooldown)
Frozen-state probes unchanged: tree clean, `git rev-parse HEAD:public` = `bee60a73` == anchor (v15 guard, 10th clean re-verify; used the tree-hash probe per v18, not the commit-hash probe that near-missed run187), rank-history mtime unmoved 07-20 07:31, gsc-harvest 2026-06-01 dark (E5, 51d, owner=jesse), knowledge best-practices-2026.md 07-13 fresh (9d, <30d).

Four tiers no-op:
1. **SAFE FIX** — none. v15 latent-sweep on cooldown: 2 runs off run186's full clean sweep, << 8-run re-arm (~run194). C15 site-wide em-dash cleanup queued but out-of-whitelist (copy-voice, owner=jesse).
2. **RESEARCH** — knowledge fresh.
3. **MEASURE** — byte-identical all-zero pull => dark per v14.8.
4. **IDLE**.

Did NOT: run a latent sweep (cooldown, 6 runs early), touch C15 (out-of-whitelist), or re-MEASURE (byte-identical). No new failure mode — v18 holding, no EVOLVE churn.

RESULT: IDLE
