# seo-brain runs — 2026-07-27

## run261 — IDLE (first run of new calendar day; 51st single-probe under v21 pure event-gate)

**Action:** IDLE. Byte-identical to run211-260.

**Probe (v21 pure event-gate, single-probe):**
- Tree clean (`git status --porcelain` empty).
- `HEAD:public` tree-hash `89ebbe4906e7ce5` == recorded anchor `89ebbe49` (v15/v18 TREE-hash guard) — no un-QC'd public/ content.
- public/ file-count **380 == recorded** → no new page → no structural-defect vector → single-probe sufficient, full latent-sweep NOT triggered (v21 gate held).
- rank-history.json mtime unmoved **Jul 20 09:31** → MEASURE dark (v14.8: don't re-MEASURE a byte-identical all-zero pull).
- newest gsc-harvest still **2026-06-01** → dark (E5, 56d, owner=jesse, priority 0).
- knowledge best-practices-2026.md **Jul 13** → fresh (14d < 30d).

**Four tiers no-op:**
1. SAFE FIX — none. v21 gate holds (file-count unchanged → single-probe). C15 (296 site-wide em/en dashes across 140 HTML pages) is out-of-whitelist copy-voice work, owner=jesse.
2. RESEARCH — knowledge fresh (<30d).
3. MEASURE — rank-history byte-identical all-zero, dark per v14.8; gsc-harvest aggregate dark (E5).
4. → IDLE.

**Did NOT do & why:** No re-MEASURE (mtime unmoved, no new data). No full latent-sweep (v21 gate: file-count 380 unchanged → single-probe correct). No C15 dash cleanup (out-of-whitelist, owner=jesse). E5 gsc-harvest restore remains owner=jesse.

**EVOLVE:** No churn. v21 holding, 51st clean application.

RESULT: IDLE

## run262 — IDLE (52nd single-probe under v21 pure event-gate)

**Action:** IDLE. Byte-identical to run211-261.

**Probe (v21 pure event-gate, single-probe):**
- Tree clean (`git status --porcelain` empty).
- `HEAD:public` tree-hash `89ebbe4906e7ce5` == recorded anchor `89ebbe49` (v15/v18 TREE-hash guard) — no un-QC'd public/ content.
- public/ file-count **380 == recorded** → no new page → no structural-defect vector → single-probe sufficient, full latent-sweep NOT triggered (v21 gate held).
- rank-history.json mtime unmoved **Jul 20 09:31** → MEASURE dark (v14.8: don't re-MEASURE a byte-identical all-zero pull).
- newest gsc-harvest still **2026-06-01** → dark (E5, 56d, owner=jesse, priority 0).
- knowledge best-practices-2026.md **Jul 13** → fresh (14d < 30d).

**Four tiers no-op:**
1. SAFE FIX — none. v21 gate holds (file-count unchanged → single-probe). C15 (296 site-wide em/en dashes across 140 HTML pages) is out-of-whitelist copy-voice work, owner=jesse.
2. RESEARCH — knowledge fresh (<30d).
3. MEASURE — rank-history byte-identical all-zero, dark per v14.8; gsc-harvest aggregate dark (E5).
4. → IDLE.

**Did NOT do & why:** No re-MEASURE (mtime unmoved, no new data). No full latent-sweep (v21 gate: file-count 380 unchanged → single-probe correct). No C15 dash cleanup (out-of-whitelist, owner=jesse). E5 gsc-harvest restore remains owner=jesse.

**EVOLVE:** No churn. v21 holding, 52nd clean application.

RESULT: IDLE

---

## run263 — IDLE (53rd single-probe under v21 pure event-gate)

**Measured:** Frozen state byte-identical to run211-262. Tree clean; `HEAD:public` 89ebbe49 == recorded anchor; public/ file-count 380 == recorded (no new page → no structural-defect vector → v21 gate holds, single-probe sufficient, full sweep NOT triggered); rank-history mtime unmoved 2026-07-20 09:31 (MEASURE dark, v14.8); gsc-harvest 2026-06-01 dark (E5, 56d, owner=jesse); knowledge 07-13 fresh (14d).

**Four tiers no-op:** SAFE FIX none (v21 gate holds; C15 296 site-wide dashes out-of-whitelist, owner=jesse) · RESEARCH fresh · MEASURE dark → IDLE.

**Did NOT do & why:** No re-MEASURE (mtime unmoved, no new data). No full latent-sweep (v21 gate: file-count 380 unchanged → single-probe correct). No C15 dash cleanup (owner=jesse). E5 gsc-harvest restore remains owner=jesse.

**EVOLVE:** No churn. v21 holding, 53rd clean application.

RESULT: IDLE

---

## run264 — IDLE (54th single-probe under v21 pure event-gate)

**Measured:** Frozen state byte-identical to run211-263. Tree clean; `HEAD:public` 89ebbe49 == recorded anchor; public/ file-count 380 == recorded (no new page → no structural-defect vector → v21 gate holds, single-probe sufficient, full sweep NOT triggered); rank-history mtime unmoved 2026-07-20 09:31 (MEASURE dark, v14.8); gsc-harvest 2026-06-01 dark (E5, 56d, owner=jesse); knowledge 07-13 fresh (14d).

**Four tiers no-op:** SAFE FIX none (v21 gate holds; C15 296 site-wide dashes out-of-whitelist, owner=jesse) · RESEARCH fresh · MEASURE dark → IDLE.

**Did NOT do & why:** No re-MEASURE (mtime unmoved, no new data). No full latent-sweep (v21 gate: file-count 380 unchanged → single-probe correct). No C15 dash cleanup (owner=jesse). E5 gsc-harvest restore remains owner=jesse.

**EVOLVE:** No churn. v21 holding, 54th clean application.

RESULT: IDLE

## run265 — IDLE (55th single-probe under v21 pure event-gate)

Byte-identical to run211-264. Probes: tree clean; HEAD:public `89ebbe49` == recorded anchor (v15/v18 TREE-hash guard); public/ file-count 380 == recorded (no new page → no structural-defect vector → single-probe sufficient, full sweep NOT triggered per v21 gate); rank-history mtime unmoved 2026-07-20 09:31 (MEASURE dark v14.8); gsc-harvest 2026-06-01 dark (E5, 56d, owner=jesse); knowledge 07-13 fresh (14d).

Four tiers no-op: SAFE FIX none (v21 gate holds; C15 296 site-wide dashes out-of-whitelist owner=jesse), RESEARCH fresh, MEASURE byte-identical all-zero dark → IDLE.

**EVOLVE:** No churn. v21 holding, 55th clean application.

RESULT: IDLE

## run266 — IDLE (56th single-probe under v21 pure event-gate)

Byte-identical to run211-265. Probes: tree clean; HEAD:public `89ebbe49` == recorded anchor (v15/v18 TREE-hash guard); public/ file-count 380 == recorded (no new page → no structural-defect vector → single-probe sufficient, full sweep NOT triggered per v21 gate); rank-history mtime unmoved 2026-07-20 09:31 (MEASURE dark v14.8); gsc-harvest 2026-06-01 dark (E5, 56d, owner=jesse); knowledge 07-13 fresh (14d).

Four tiers no-op: SAFE FIX none (v21 gate holds; C15 296 site-wide dashes out-of-whitelist owner=jesse), RESEARCH fresh, MEASURE byte-identical all-zero dark → IDLE.

**EVOLVE:** No churn. v21 holding, 56th clean application.

RESULT: IDLE

## run267 — IDLE (57th single-probe under v21 pure event-gate)

Byte-identical to run211-266. Probes: tree clean; HEAD:public `89ebbe49` == recorded anchor (v15/v18 TREE-hash guard); public/ file-count 380 == recorded (no new page → no structural-defect vector → single-probe sufficient, full sweep NOT triggered per v21 gate); rank-history mtime unmoved 2026-07-20 09:31 (MEASURE dark v14.8); gsc-harvest 2026-06-01 dark (E5, 56d, owner=jesse); knowledge 07-13 fresh (14d).

Four tiers no-op: SAFE FIX none (v21 gate holds; C15 296 site-wide dashes out-of-whitelist owner=jesse), RESEARCH fresh, MEASURE byte-identical all-zero dark → IDLE.

**EVOLVE:** No churn. v21 holding, 57th clean application.

RESULT: IDLE

## run268 — IDLE (58th single-probe under v21 pure event-gate)

Byte-identical to run211-267. Probes: tree clean; HEAD:public `89ebbe49` == recorded anchor (v15/v18 TREE-hash guard); public/ file-count 380 == recorded (no new page → no structural-defect vector → single-probe sufficient, full sweep NOT triggered per v21 gate); rank-history mtime unmoved 2026-07-20 09:31 (MEASURE dark v14.8); gsc-harvest 2026-06-01 dark (E5, 56d, owner=jesse); knowledge 07-13 fresh (14d).

Four tiers no-op: SAFE FIX none (v21 gate holds; C15 296 site-wide dashes out-of-whitelist owner=jesse), RESEARCH fresh, MEASURE byte-identical all-zero dark → IDLE.

**EVOLVE:** No churn. v21 holding, 58th clean application.

RESULT: IDLE

## run269 — IDLE (59th single-probe under v21 pure event-gate)

Byte-identical to run211-268. Probes: tree clean; HEAD:public `89ebbe49` == recorded anchor (v15/v18 TREE-hash guard); public/ file-count 380 == recorded (no new page → no structural-defect vector → single-probe sufficient, full sweep NOT triggered per v21 gate); rank-history mtime unmoved 2026-07-20 09:31 (MEASURE dark v14.8); gsc-harvest 2026-06-01 dark (E5, 56d, owner=jesse); knowledge 07-13 fresh (14d).

Four tiers no-op: SAFE FIX none (v21 gate holds; C15 296 site-wide dashes out-of-whitelist owner=jesse), RESEARCH fresh, MEASURE byte-identical all-zero dark → IDLE.

**EVOLVE:** No churn. v21 holding, 59th clean application.

RESULT: IDLE

## run270 — IDLE (60th single-probe under v21 pure event-gate)

Byte-identical to run211-269. Probes: tree clean; HEAD:public `89ebbe4` == recorded anchor (v15/v18 TREE-hash guard); public/ file-count 380 == recorded (no new page → no structural-defect vector → single-probe sufficient, full sweep NOT triggered per v21 gate); rank-history mtime unmoved 2026-07-20 09:31 (MEASURE dark v14.8); gsc-harvest 2026-06-01 dark (E5, 56d, owner=jesse); knowledge 07-13 fresh (14d).

Four tiers no-op: SAFE FIX none (v21 gate holds; C15 296 site-wide dashes out-of-whitelist owner=jesse), RESEARCH fresh, MEASURE byte-identical all-zero dark → IDLE.

**EVOLVE:** No churn. v21 holding, 60th clean application.

RESULT: IDLE

## run271 — IDLE (MEASURE-candidate evaluated → held dark v14.8; weekly-rank-watch fired 3rd consecutive all-zero Monday)

**Not a pure frozen probe this run.** rank-history.json mtime **ADVANCED Jul 20 09:31 → Jul 27 09:31**: `net.jessetek.weekly-rank-watch` fired its Monday run (today is Mon 2026-07-27) and appended a NEW distinct ISO date **2026-07-27** for all 10 tracked queries. Per the v14.3 horizon-edge guard, treated as a MEASURE-candidate and ran the disambiguation, NOT an auto-IDLE.

**MEASURE evaluation:**
- ISO-date scan: `[..., 2026-06-01, 2026-07-13, 2026-07-20, 2026-07-27]` — 07-27 is new.
- Verified all 10 queries' 07-27 entry: **every reading null/0-impressions** (python check: `non-zero 07-27 readings: NONE`). Byte-identical in VALUE to the 07-20 and 07-13 all-zero pulls.
- Per **v14.8**: do NOT re-MEASURE / re-snapshot an all-zero pull byte-identical to the last recorded reading — a moved mtime alone is not new *information*. This is now the **3rd consecutive Monday** (07-13, 07-20, 07-27) of empty-window pulls, strongly confirming the `get-gsc-rank.mjs` empty-window / GSC-finalization-lag artifact hypothesis, NOT a ranking collapse. The 2026-06-01 aggregate independently proved real visibility (169 impr / 4 clicks, branded avg pos ~2.5). Did NOT report a loss to Jesse.
- MEASURE → **dark**. Disambiguation (E5 restore of weekly-gsc-harvest, or fix get-gsc-rank.mjs windowing) is owner=jesse, priority 0.

**Other probes (frozen):** tree clean; `HEAD:public` `89ebbe4` == recorded anchor (v15/v18 TREE-hash guard); public/ file-count **380 == recorded** (no new page → no structural-defect vector → single-probe sufficient, v21 gate holds, full sweep NOT triggered); gsc-harvest still **2026-06-01** dark (E5, 56d, owner=jesse); knowledge best-practices-2026.md **07-13** fresh (14d < 30d).

**Four tiers no-op:** SAFE FIX none (v21 gate holds; C15 296 site-wide dashes out-of-whitelist owner=jesse) · RESEARCH fresh · MEASURE dark (07-27 all-zero, v14.8) → IDLE.

**Did NOT do & why:** No re-snapshot of the 07-27 all-zero pull (v14.8 — no new information). No loss report to Jesse (empty-window artifact, not a collapse). No full latent-sweep (v21 gate: file-count 380 unchanged). No C15 dash cleanup (owner=jesse). E5 gsc-harvest restore remains owner=jesse.

**EVOLVE:** No churn. v14.8 held correctly on its 3rd live Monday trigger (the fire-detector + all-zero guard working exactly as designed across 07-13/07-20/07-27). v21 structural gate holding, 61st clean application.

RESULT: IDLE

## run272 — IDLE (62nd single-probe under v21 pure event-gate; first frozen re-verify after run271's 07-27 MEASURE-candidate)

**Pure frozen probe.** rank-history.json mtime unmoved **Jul 27 09:31** (== run271; the 07-27 all-zero Monday pull was already evaluated + held dark at run271 per v14.8) → no new information → MEASURE dark.

**Probe (v21 pure event-gate, single-probe):**
- Tree clean (`git status --porcelain` empty).
- `HEAD:public` tree-hash `89ebbe4906e7ce5` == recorded anchor `89ebbe49` (v15/v18 TREE-hash guard) — no un-QC'd public/ content.
- public/ file-count **380 == recorded** → no new page → no structural-defect vector → single-probe sufficient, full latent-sweep NOT triggered (v21 gate held).
- rank-history.json mtime unmoved **Jul 27 09:31** → MEASURE dark (v14.8: don't re-MEASURE a byte-identical all-zero pull).
- newest gsc-harvest still **2026-06-01** → dark (E5, 56d, owner=jesse, priority 0).
- knowledge best-practices-2026.md **Jul 13** → fresh (14d < 30d).

**Four tiers no-op:** SAFE FIX none (v21 gate holds; C15 296 site-wide dashes out-of-whitelist owner=jesse) · RESEARCH fresh · MEASURE byte-identical all-zero dark → IDLE.

**Did NOT do & why:** No re-MEASURE (mtime unmoved from run271, no new data). No full latent-sweep (v21 gate: file-count 380 unchanged → single-probe correct). No C15 dash cleanup (owner=jesse). E5 gsc-harvest restore remains owner=jesse.

**EVOLVE:** No churn. v21 holding, 62nd clean application.

RESULT: IDLE
