# 2026-07-21 — SEO Brain runs

## run178 — SHIPPED C14: fixed malformed image sitemap (5 unescaped ampersands)

**Decision: SHIP.** Latent-sweep re-armed at run178 (8 runs off run170's ship), so I ran a full latent-defect QC pass over committed `public/` instead of another single-probe — and it caught a real defect the fast-path structurally cannot see.

- **Defect:** `public/sitemap-images.xml` carried 5 raw unescaped `&` chars in image titles/captions (`LA & OC`, `Downey & Surrounding Cities`, `LA & Orange County`, `Reviews & Client Stories`, `Videos & Reels`). Result: `xmllint` failed with `xmlParseEntityRef: no name` — the **entire image sitemap was malformed XML**. Google discards a sitemap it can't parse, so every image entry (referenced from `sitemap-index.xml`) was invisible for Google Images. Same class as run170's future-lastmod: a crawl-signal loss buried in already-committed content.
- **Fix:** escaped all 5 → `&amp;`. QC gate: `xmllint` now **well-formed** (was BAD); all 4 site XML files valid; diff exactly 5+/5-, one file, ampersands only; 0 bare `&` remain. Whitelisted ("sitemap updates") → committed `5bbc6e6` and pushed to origin main.
- **RESEARCH** — knowledge 2026-07-13 (8d) fresh, skipped. **MEASURE** — rank-history mtime unmoved 07-20 07:31, reading_2026-07-20 already recorded, aggregate dark (E5) → dark per v14.8.
- **ANCHOR ROTATED (v15 guard):** this push touched `public/`, so `HEAD:public` moved `0012fbc7` → **`bee60a73`**. Future single-probes compare against `bee60a73`. Cooldown reset — next latent-sweep re-arm ≈run186.

**Notable / did NOT:** The sweep also flagged **53 em/en dashes** in `sitemap-images.xml` (e.g. `Jesse Oñate — bilingual realtor…` captions). Image-sitemap captions can surface in Google Images, so the human-voice rule arguably applies — but rewriting 53 copy strings is a copy change outside the SAFE-FIX whitelist and beyond one small step. Logged as **C15 (em-dash cleanup in image sitemap captions), status queued** rather than expanding scope this run. No other latent defects found: sitemap.xml lastmods all past/valid (run170 holding), 0 aggregateRating, 0 "100+ reviews" regressions, 5 future-dated `/insights/` posts are intentional themed-month blog slugs (not defects).

## run177 — IDLE (seventh frozen re-verify after run170's ship; no state change)

**Decision: IDLE.** Single-probe four-tier sweep, all no-op — byte-identical to run176.

- **SAFE FIX** — none. Tree clean; `HEAD:public` = `0012fbc7` == post-run170 anchor; HEAD == committed, nothing un-QC'd. v14.9 latent-sweep on **cooldown** per v15: 7 idle runs off run170's ship (171-176 + this), just under the ~8-run re-arm. **run178 reaches the re-arm threshold** — next run may re-arm the latent-defect sweep.
- **RESEARCH** — `knowledge/best-practices-2026.md` 2026-07-13 (8d, <30d) → fresh.
- **MEASURE** — rank-history mtime **still 2026-07-20 07:31** (unmoved); newest reading `reading_2026-07-20`, all-zero, already recorded. Aggregate harvest still `2026-06-01` (50d stale, E5, owner=jesse). Per v14.8, no re-snapshot of a byte-identical all-zero pull → dark.
- → **IDLE**.

**Notable:** Nothing advanced since run176. rank-watch's next fire is Monday 07-27; aggregate harvest stays dark until E5 (owner=jesse). Latent-sweep re-arms at run178 — worth a full latent-defect pass then rather than another single-probe.

**Did NOT:** ship (nothing un-QC'd, sweep cooldown); re-run latent sweep (v15, 7 runs off ship < 8-run re-arm); kickstart launchd (E5, owner=jesse); re-MEASURE identical all-zero pull (v14.8).

## run176 — IDLE (sixth frozen re-verify after run170's ship; no state change)

**Decision: IDLE.** Single-probe four-tier sweep, all no-op.

- **SAFE FIX** — none. Tree clean; `HEAD:public` = `0012fbc` == post-run170 anchor. HEAD == committed, nothing un-QC'd. v14.9 latent-sweep still on **cooldown** per v15: 6 idle runs off run170's ship (171-175 + this), under the ~8-run re-arm threshold.
- **RESEARCH** — `knowledge/best-practices-2026.md` 2026-07-13 (8d, <30d) → fresh.
- **MEASURE** — rank-history mtime **still 2026-07-20 07:31** (unmoved); newest reading `reading_2026-07-20`, all-zero, already recorded. Aggregate harvest still `2026-06-01` (50d stale, E5, owner=jesse). Per v14.8, no re-snapshot of a byte-identical all-zero pull → dark.
- → **IDLE**.

**Notable:** Nothing advanced since run175 this morning. rank-watch's next fire is Monday 07-27; aggregate harvest stays dark until E5 (owner=jesse). Same four idle gates as runs 171-175.

**Did NOT:** ship (nothing un-QC'd, sweep cooldown); re-run latent sweep (v15, 6 runs off ship < 8-run re-arm); kickstart launchd (E5, owner=jesse); re-MEASURE identical all-zero pull (v14.8).

## run175 — IDLE (fifth frozen re-verify after run170's ship; new calendar day, no state change)

**Decision: IDLE.** Full four-tier priority sweep, all evaluate to no-op.

- **SAFE FIX** — none. `git status --porcelain` empty (tree clean); `git rev-parse HEAD:public` = `0012fbc714533631fcc1067c5e1891807ff98072` == the post-run170 anchor (rotated from `77a63300` when run170's sitemap fix pushed public/). HEAD == committed, so nothing un-QC'd in public/. The v14.9 latent-defect sweep remains on **cooldown** per v15: run170 swept + shipped only 5 idle runs ago (171-174 + this run); re-arm threshold is ~8+ idle runs re-accumulating. Nothing whitelisted shippable.
- **RESEARCH** — `knowledge/best-practices-2026.md` dated 2026-07-13 (8 days old, <30d) → fresh, no forced open question.
- **MEASURE** — `automation/.state/rank-history.json` mtime **still 2026-07-20 07:31** (unmoved since run160 captured the Monday pull); newest reading is `reading_2026-07-20`, an all-zero per-query pull already recorded. Aggregate `gsc-harvest` still `2026-06-01` (50d stale, E5 REMAINING, owner=jesse). Per v14.8, do NOT re-snapshot an all-zero pull byte-identical to the last recorded reading → MEASURE dark.
- → **IDLE**.

**Notable:** First run of a new calendar day (07-21). Nothing in the pipeline advanced overnight — rank-watch's next fire is the coming Monday (07-27), aggregate harvest stays dark until E5 (owner=jesse). All four idle gates are the same as runs 171-174.

**Did NOT:** ship any public/ change (nothing un-QC'd, sweep on cooldown); re-run the latent-defect sweep (v15 cooldown, 5 runs off ship < 8-run re-arm); kickstart any launchd job (E5, owner=jesse); re-MEASURE the identical all-zero Monday pull (v14.8).

**Frozen state (all re-verified, unchanged):** HEAD:public == `0012fbc7` == anchor + tree clean; knowledge 07-13 fresh; rank-watch monitor ALIVE (last Monday pull 07-20 captured); aggregate weekly-gsc-harvest dark at `gsc-harvest-2026-06-01.json` (E5, owner=jesse, priority 0). Every run in this streak is provably IDLE before it starts (E6 cost-throttle case, owner=jesse). Per-run detail lives here; ledger keeps only the compact tally.
