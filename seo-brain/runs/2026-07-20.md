# 2026-07-20 — SEO Brain runs

## run152 — IDLE (frozen-state re-verify; weekly fire day, not yet fired)

**Decision: IDLE.** Full four-tier priority sweep, all evaluate to no-op.

- **SAFE FIX** — none. `git status --porcelain` empty (tree clean); `git rev-parse HEAD:public` = `77a63300813073a0d83a37b3d3397ee5b0180fe4` == last-qc.json anchor (public_tree_hash) == unchanged since run41 re-anchor. No un-QC'd content in public/ → nothing in the whitelist is shippable (per run59 audit; still holds).
- **RESEARCH** — `knowledge/best-practices-2026.md` mtime Jul 13 (7 days old, <30d) → still fresh, no forced open question.
- **MEASURE** — `automation/.state/rank-history.json` mtime **Jul 13 15:06** (unchanged); distinct dates end at 2026-07-13, already captured in rank-snapshot.json `reading_2026-07-13`. No NEW GSC data → MEASURE dark.
- → **IDLE**.

**Notable this run:** Today (07-20 Mon) is the day run60-horizon expected `net.jessetek.weekly-rank-watch` to fire. It IS loaded (`launchctl list` shows `net.jessetek.weekly-rank-watch`, last exit 0) but has **not re-fired yet** — rank-history mtime is still 07-13 15:06, so no fresh pull today. It fires on its own launchd schedule; I did NOT manually kickstart it (that's E5, owner=jesse pipeline, outside the on-page SAFE-FIX whitelist, and a lag-empty window is likely anyway). The first hourly run *after* rank-history's mtime advances past 07-13 is the first MEASURE candidate since 07-13 — must re-scan rank-history for a NEW distinct date, caveat GSC ~2-3d finalization-lag empty-window artifact (see rank-snapshot.json / E5 caveat).

**Did NOT:** trigger the launchd job (owner=jesse); ship any public/ change (nothing un-QC'd); run the full QC battery (fast-path: anchor equal + tree clean + knowledge fresh → prior CLEAN verdict from run41 still governs).

**Frozen state (all re-verified, unchanged since run55):** weekly-rank-watch loaded; aggregate weekly-gsc-harvest still dark at gsc-harvest-2026-06-01.json (E5 REMAINING, owner=jesse, priority 0); HEAD:public==anchor, tree clean; knowledge fresh. Every run in this horizon is provably IDLE before it starts (E6 cost-throttle case, owner=jesse). Per-run detail lives here; ledger keeps only the compact tally.

## run153 — IDLE (fire-day re-verify; weekly-rank-watch STILL not fired ~same hour)

**Decision: IDLE.** Same four-tier sweep, same no-op verdict as run152.

- **SAFE FIX** — none. Tree clean (`git status --porcelain` empty); `git rev-parse HEAD:public` = `77a63300813073a0d83a37b3d3397ee5b0180fe4` == anchor. No un-QC'd content → nothing whitelisted shippable.
- **RESEARCH** — `knowledge/best-practices-2026.md` still Jul 13 (7d, <30d) → fresh.
- **MEASURE** — `rank-history.json` mtime **still Jul 13 15:06** (unchanged from run152); newest distinct date still 2026-07-13, already in rank-snapshot `reading_2026-07-13`. MEASURE dark.
- → **IDLE**.

**Notable:** Now well into fire-day 07-20 and `net.jessetek.weekly-rank-watch` still has NOT written a new pull (mtime unmoved from 07-13). Two live possibilities, both consistent with IDLE: (a) the launchd schedule fires later in the day / on a calendar interval I don't need to second-guess, or (b) the job's next-fire is not literally 07-20 (the "~07-20" horizon was an estimate). Either way the correct move is unchanged: do NOT kickstart (E5 owner=jesse), just keep re-scanning rank-history mtime each run. **Guard reinforced (v14.2 fire-day MEASURE protocol):** mtime is the authoritative fire-detector, not the calendar estimate — a horizon date passing without an mtime advance is NOT a reason to intervene; it's still just IDLE until the file actually changes.

**Did NOT:** kickstart the job; ship anything; run full QC (fast-path governs — anchor equal + clean + fresh).

## run154 — IDLE (fire-day re-verify #3; horizon estimate now falsified, not fired)

**Decision: IDLE.** Same four-tier sweep, same no-op.

- **SAFE FIX** — none. `git status --porcelain` empty; `git rev-parse HEAD:public` = `77a63300813073a0d83a37b3d3397ee5b0180fe4` == last-qc.json anchor. No un-QC'd content shippable.
- **RESEARCH** — `knowledge/best-practices-2026.md` mtime Jul 13 09:29 (7d, <30d) → fresh.
- **MEASURE** — `rank-history.json` mtime **still Jul 13 15:06** (unmoved from run152/153); newest distinct date still 2026-07-13 == snapshot `reading_2026-07-13`. MEASURE dark.
- → **IDLE**.

**Notable:** Third consecutive re-verify on 07-20 with weekly-rank-watch still not writing (mtime frozen at 07-13 15:06). The "~07-20" fire horizon (a run60 estimate) is now empirically **falsified** — the day is well advanced and the job has not written. Per v14.2 this is correctly still IDLE (mtime is the authoritative fire-detector, not the calendar). EVOLVE v14.4 below stops the loop from re-asserting "today IS fire day" now that 07-20 has passed without a write — the next fire is simply "unknown, whenever mtime advances," so future runs should not keep re-deriving/citing a specific expected date. Prevents the phantom-precision churn that runs 152→154 spent narrating.

**Did NOT:** kickstart the job (E5 owner=jesse); ship anything; run full QC (fast-path governs).

## run155 — IDLE (frozen-state re-verify; first clean v14.4 application)

**Decision: IDLE.** Same four-tier sweep, same no-op. First run governed by v14.4 — no calendar fire-date is asserted; the only fire-detector is the rank-history mtime, and it has not moved.

- **SAFE FIX** — none. `git status --porcelain` empty; `git rev-parse HEAD:public` = `77a63300813073a0d83a37b3d3397ee5b0180fe4` == last-qc.json anchor. No un-QC'd content shippable.
- **RESEARCH** — `knowledge/best-practices-2026.md` mtime Jul 13 09:29 (7d, <30d) → fresh.
- **MEASURE** — `rank-history.json` mtime **still Jul 13 15:06** (unmoved); newest distinct date still 2026-07-13 == snapshot `reading_2026-07-13`. MEASURE dark.
- → **IDLE**.

**Notable:** Consolidated the entire idle pre-check into ONE deterministic Bash probe this run (anchor tree hash == last-qc + newest rank date + knowledge mtime in a single call) instead of the multi-command narration prior runs used — keeps the idle run near-free per the mandate. No date estimate re-derived (v14.4). Next MEASURE candidate is simply the first hourly run after rank-history mtime advances past 07-13; nothing to predict or intervene on until then.

**Did NOT:** kickstart the job (E5 owner=jesse); ship anything; run full QC (fast-path governs — anchor equal + clean + fresh).

## run156 — IDLE (frozen-state re-verify; single-probe fast-path)

**Decision: IDLE.** One deterministic probe (v14.5), all four tiers no-op.

- **SAFE FIX** — none. `git status --short` empty; `git rev-parse HEAD:public` = `77a63300813073a0d83a37b3d3397ee5b0180fe4` == last-qc.json anchor. No un-QC'd content shippable.
- **RESEARCH** — `knowledge/best-practices-2026.md` mtime Jul 13 09:29 (7d, <30d) → fresh.
- **MEASURE** — `rank-history.json` mtime **still Jul 13 15:06** (unmoved); newest distinct date still 2026-07-13 == snapshot `reading_2026-07-13`. MEASURE dark.
- → **IDLE**. No fire-date asserted (v14.4); next MEASURE candidate = first run after rank-history mtime advances past 07-13.

**Did NOT:** kickstart the job (E5 owner=jesse); ship anything; run full QC (fast-path governs). No date estimate re-derived.

## run157 — IDLE (frozen; single-probe v14.6)
Probe: `HEAD:public`=`77a63300`==anchor, tree clean, rank-history mtime **still 07-13 15:06** (MEASURE dark), knowledge 07-13 (fresh, 7d). All four tiers no-op → IDLE. Applying v14.7: repeat frozen-IDLE runs collapse to a one-line log entry (this line) rather than a full block, to stop same-day run-log bloat. Did NOT: kickstart (E5 owner=jesse), ship, or run full QC.

## run158 — IDLE (frozen; single-probe v14.7 one-liner)
Probe: `HEAD:public`=`77a63300`==anchor, tree clean, rank-history mtime **still 07-13 15:06** (MEASURE dark, newest distinct date 2026-07-13 already in snapshot), knowledge 07-13 (fresh, 7d). All four tiers no-op → IDLE. Sixth consecutive same-day frozen-IDLE (runs152-158, all 07-20); collapsed to one line per v14.7. Did NOT: kickstart (E5 owner=jesse), ship, or run full QC.

## run159 — IDLE (frozen; single-probe v14.7 one-liner)
Probe: `HEAD:public`=`77a63300`==anchor, tree clean, rank-history mtime **still 07-13 15:06** (MEASURE dark, newest distinct date 2026-07-13 already in snapshot), knowledge 07-13 (fresh, 7d). All four tiers no-op → IDLE. Seventh consecutive same-day frozen-IDLE (runs152-159, all 07-20); collapsed to one line per v14.7 (no EVOLVE churn — v14.7 holding). Did NOT: kickstart (E5 owner=jesse), ship, or run full QC.

## run160 — MEASURE (frozen streak broken; weekly-rank-watch fired 07-20 07:31)

**Decision: MEASURE.** The single-probe fast-path detected the state change v14.7 was waiting for: **rank-history.json mtime advanced from 07-13 15:06 → 2026-07-20 07:31**. The Monday `net.jessetek.weekly-rank-watch` job finally fired (second successful run since the 07-13 restore) and appended a NEW distinct date, **2026-07-20**, for all 10 tracked queries. That is genuine new GSC data not yet in the snapshot → MEASURE fires ahead of IDLE.

- **SAFE FIX** — none. `git status` clean; `HEAD:public` = `77a63300…` == anchor. No un-QC'd content shippable. Ledger has no input-complete Claude-owned whitelist item (queued C2-C7/E1-E2 are content tasks needing Jesse's real local data; the awaiting-jesse/in-progress items are owner=jesse). So MEASURE is the correct tier.
- **The reading (2026-07-20, source=gsc, all 10 queries):** rank=null, impressions=0 for every query — **including branded `realtor jesse onate`.**
- **Escalation threshold met, but held honest.** The 07-13 snapshot set the rule: *if branded is still 0 on the next Monday run, escalate as a genuine indexing problem.* It IS still 0. BUT the 2026-06-01 aggregate harvest independently measured 169 impressions / 4 clicks with branded at avg pos ~2.5 — the site was demonstrably visible 7 weeks ago. Two identical all-zero per-query pulls a week apart (07-13, 07-20), branded included, most plausibly indicate `get-gsc-rank.mjs` is querying an unsettled/empty GSC window every run (GSC 2-3d finalization latency), NOT a real ranking collapse. **I did NOT report a ranking loss to Jesse** off this ambiguous signal (would violate the no-invented-facts rule).
- **Snapshot updated** → `reading_2026-07-20`, `delta_vs_2026-07-13` (no change), escalation_flag, and a `harvester_windowing_suspect` note. Disambiguation is owner=jesse (E5 restore of weekly-gsc-harvest, or a look at get-gsc-rank.mjs windowing) — NOT a Claude-safe auto-fix.

**Notable:** This is the first non-IDLE run since ~run55. The v14.7 mtime-fire-detector worked exactly as designed — 8 near-free frozen probes (152-159) then an instant, cheap MEASURE the moment the file moved. Guard added below (v14.8) to stop re-snapshotting identical all-zero pulls in future weeks.

**Did NOT:** ship any public/ change; kickstart or edit the launchd job or get-gsc-rank.mjs (owner=jesse, E5); fabricate a rank or report a visibility loss; run full QC (no public/ change — fast-path anchor==clean==fresh governs).

---

### Frozen-IDLE tally (post-MEASURE re-verifies, v14.7 one-line collapse)
- **run161 — IDLE.** Single-probe: `HEAD:public`=77a6330==anchor, tree clean, rank-history mtime unmoved at 07-20 07:31, snapshot already=`reading_2026-07-20`. Per v14.8 the 07-20 all-zero pull is already recorded → no re-MEASURE of a byte-identical reading. MEASURE dark until a non-zero pull or the aggregate weekly-gsc-harvest (E5) returns; knowledge fresh (<30d); no shippable SAFE FIX. First frozen re-verify after run160's MEASURE.
- **run162 — IDLE.** Single-probe: `HEAD:public`=77a6330==anchor, tree clean, rank-history mtime unmoved at 07-20 07:31, snapshot already=`reading_2026-07-20`, knowledge 07-13 (fresh, 7d). All four tiers no-op (SAFE FIX none / RESEARCH fresh / MEASURE byte-identical per v14.8 / → IDLE). Second frozen re-verify after run160's MEASURE; v14.7 one-line collapse holding, no EVOLVE churn. Did NOT: kickstart (E5 owner=jesse), ship, or re-snapshot.
- **run163 — IDLE.** Single-probe: `HEAD:public`=77a63300==anchor, tree clean, rank-history mtime unmoved at 07-20 07:31, snapshot already=`reading_2026-07-20`, knowledge 07-13 (fresh, 7d). All four tiers no-op (SAFE FIX none / RESEARCH fresh / MEASURE byte-identical per v14.8 / → IDLE). Third frozen re-verify after run160's MEASURE; v14.7 one-line collapse holding, no EVOLVE churn. Did NOT: kickstart (E5 owner=jesse), ship, or re-snapshot.
- **run164 — IDLE.** Single-probe: `HEAD:public`=77a63300==anchor, tree clean, rank-history mtime unmoved at 07-20 07:31, snapshot already=`reading_2026-07-20`, newest gsc-harvest still 2026-06-01, knowledge 07-13 (fresh, 7d). All four tiers no-op (SAFE FIX none / RESEARCH fresh / MEASURE byte-identical per v14.8 / → IDLE). Fourth frozen re-verify after run160's MEASURE; v14.7 one-line collapse holding, no EVOLVE churn. Did NOT: kickstart (E5 owner=jesse), ship, or re-snapshot.
- **run165 — IDLE.** Single-probe: `HEAD:public`=77a63300==anchor, tree clean, rank-history mtime unmoved at 07-20 07:31, snapshot already=`reading_2026-07-20`, newest gsc-harvest still 2026-06-01, knowledge 07-13 (fresh, 7d). All four tiers no-op (SAFE FIX none / RESEARCH fresh / MEASURE byte-identical per v14.8 / → IDLE). Fifth frozen re-verify after run160's MEASURE; v14.7 one-line collapse holding, no EVOLVE churn. Did NOT: kickstart (E5 owner=jesse), ship, or re-snapshot.
- **run166 — IDLE.** Single-probe: `HEAD:public`=77a63300==anchor, tree clean, rank-history mtime unmoved at 07-20 07:31, snapshot already=`reading_2026-07-20` (newest distinct date 2026-07-20 already captured), newest gsc-harvest still 2026-06-01, knowledge 07-13 (fresh, 7d). All four tiers no-op (SAFE FIX none / RESEARCH fresh / MEASURE byte-identical per v14.8 / → IDLE). Sixth frozen re-verify after run160's MEASURE; v14.7 one-line collapse holding, no EVOLVE churn. Did NOT: kickstart (E5 owner=jesse), ship, or re-snapshot.
- **run167 — IDLE.** Single-probe: `HEAD:public`=77a63300==anchor, tree clean, rank-history mtime unmoved at 07-20 07:31, snapshot already=`reading_2026-07-20`, newest gsc-harvest still 2026-06-01, knowledge 07-13 (fresh, 7d). All four tiers no-op (SAFE FIX none / RESEARCH fresh / MEASURE byte-identical per v14.8 / → IDLE). Seventh frozen re-verify after run160's MEASURE; v14.7 one-line collapse holding, no EVOLVE churn. Did NOT: kickstart (E5 owner=jesse), ship, or re-snapshot.
- **run168 — IDLE.** Single-probe: `HEAD:public`=77a63300==anchor, tree clean, rank-history mtime unmoved at 07-20 07:31, snapshot already=`reading_2026-07-20`, newest gsc-harvest still 2026-06-01, knowledge 07-13 (fresh, 7d). All four tiers no-op (SAFE FIX none / RESEARCH fresh / MEASURE byte-identical per v14.8 / → IDLE). Eighth frozen re-verify after run160's MEASURE; v14.7 one-line collapse holding, no EVOLVE churn. Did NOT: kickstart (E5 owner=jesse), ship, or re-snapshot.
- **run169 — IDLE.** Single-probe: `HEAD:public`=77a63300==anchor, tree clean, rank-history mtime unmoved at 07-20 07:31, snapshot already=`reading_2026-07-20`, newest gsc-harvest still 2026-06-01, knowledge 07-13 (fresh, 7d). All four tiers no-op (SAFE FIX none / RESEARCH fresh / MEASURE byte-identical per v14.8 / → IDLE). Ninth frozen re-verify after run160's MEASURE; v14.7 one-line collapse holding, no EVOLVE churn. Did NOT: kickstart (E5 owner=jesse), ship, or re-snapshot.

---

## run170 — SHIPPED (sitemap lastmod defect fix; broke the frozen-IDLE streak)

**Decision: SAFE FIX (priority 1).** Instead of a tenth blind single-probe re-verify, ran the latent-defect sweep v14.7's fast-path had never triggered: the `HEAD:public==anchor` check only proves no *un-QC'd NEW* content slipped in — it says nothing about latent SEO defects sitting in *already-committed* files. Audited a whitelist item that needs zero Jesse input (sitemap correctness) and found a real one.

- **Defect:** `public/sitemap.xml` carried **5 future-dated `<lastmod>` values** (2026-08-06, 09-03, 09-10, 10-01, 10-15) while today is 2026-07-20. All 5 are on live `/insights/` blog posts whose slugs are themed for future months (`socal-rate-watch-september-2026`, `-october-2026`, `socal-refinance-guide-2026`, `seller-pricing-strategy-socal`, `down-payment-assistance-california`) — the generator had set `lastmod` = the post's themed month rather than its real modification date. **Why it matters:** Google treats a sitemap containing future `lastmod` values as unreliable and can discard `lastmod` for the *entire* sitemap (John Mueller, public guidance) — a self-inflicted crawl-priority signal loss, and adjacent to the no-black-hat constraint (faux-freshness dating).
- **Fix (honest, verifiable):** set all 5 to **2026-07-02**, the real last git-commit date of each file (`git log -1 --format=%cs` — identical for all 5, they shipped together). Did NOT fabricate a date, did NOT use today's date (the pages were not modified today), did NOT remove the pages (they are real, live, indexable — they belong in the sitemap).
- **QC gate passed:** `xmllint --noout public/sitemap.xml` OK; **0** remaining future-dated lastmod; `<loc>` count unchanged (151); `git diff --stat` = 5 insertions / 5 deletions on the one file only. Confirmed `sitemap-index.xml` + `sitemap-images.xml` carry no future/stale lastmod → fix is self-contained.
- **Whitelisted → auto-deployed:** "sitemap updates" is on the SAFE-FIX push whitelist; pushed to origin main.

**MEASURE/RESEARCH/other tiers:** MEASURE still byte-identical (`reading_2026-07-20`, rank-history mtime unmoved 07-20 07:31, gsc-harvest still 2026-06-01) per v14.8; knowledge fresh (07-13, 7d). SAFE FIX outranked them per priority order.

**Notable:** This is the fix the frozen streak (runs 152-169) structurally could not see. The single-probe fast-path is correct for its purpose (detecting new un-QC'd commits + MEASURE fires) but it created a blind spot for *pre-existing* latent defects. EVOLVE v14.9 below adds a low-frequency latent-defect sweep so idle streaks re-audit committed content instead of only re-checking the anchor.

**Did NOT:** touch the /insights/ page content (only sitemap metadata); alter the 9 legitimate past-dated lastmod values; kickstart the launchd job (E5 owner=jesse); re-snapshot MEASURE (byte-identical).

RESULT: SHIPPED

---

## run171 — IDLE (first frozen re-verify after run170's SAFE-FIX ship; anchor rotated)

**Decision: IDLE.** Single-probe against the NEW post-run170 state:
- Tree clean; `HEAD:public` = **`0012fbc7`** — this is a NEW anchor. run170's sitemap ship changed `public/`, so the old `77a63300` anchor is retired. Tree-clean + HEAD==committed ⇒ nothing un-QC'd.
- rank-history.json mtime unmoved at **07-20 07:31** (same as run170) ⇒ no new per-query pull.
- snapshot still `reading_2026-07-20`; newest gsc-harvest still **2026-06-01** (E5 dark, owner=jesse).
- knowledge `best-practices-2026.md` 07-13 (fresh, 7d).

Four-tier scan: **SAFE FIX** none — run170 *just* ran the v14.9 latent-defect sweep and shipped the sitemap fix; re-sweeping this hour would violate "low-frequency" (the sweep is for *long idle streaks*, and we are one run off a ship). All whitelisted claude-owned items remain done-live; the rest gate on Jesse's local content (C2/C3/C4/C6/C7) or off-page levers. **RESEARCH** — knowledge fresh (<30d). **MEASURE** — byte-identical per v14.8. → **IDLE**.

**Did NOT:** re-run the latent sweep (cooldown, see v15), ship, re-snapshot, or kickstart E5 (owner=jesse). The only durable output this run is recording the anchor rotation so future single-probes compare against `0012fbc7`, not the retired `77a63300`.

RESULT: IDLE

---

### Frozen-IDLE tally (post-run170-ship re-verifies, v15 anchor `0012fbc7`, one-line collapse)
- **run172 — IDLE.** Single-probe: `HEAD:public`=`0012fbc7`==anchor, tree clean (HEAD==committed, nothing un-QC'd), rank-history mtime unmoved at 07-20 07:31 (newest distinct date 2026-07-20 already in snapshot), snapshot `reading_2026-07-20`, newest gsc-harvest still 2026-06-01 (E5 dark, owner=jesse), knowledge 07-13 (fresh, 7d). All four tiers no-op (SAFE FIX none — latent-sweep on v15 cooldown, two runs off run170's ship / RESEARCH fresh / MEASURE byte-identical per v14.8 / → IDLE). Second frozen re-verify after run170; no EVOLVE churn (v15 holding). Did NOT: kickstart (E5 owner=jesse), ship, re-snapshot, or re-sweep.
- **run173 — IDLE.** Single-probe: `HEAD:public`=`0012fbc7`==anchor, tree clean (HEAD==committed, nothing un-QC'd), rank-history mtime unmoved at 07-20 07:31 (newest distinct date 2026-07-20 already in snapshot), snapshot `reading_2026-07-20`, newest gsc-harvest still 2026-06-01 (E5 dark, owner=jesse), knowledge 07-13 (fresh, 7d). All four tiers no-op (SAFE FIX none — latent-sweep on v15 cooldown, three runs off run170's ship / RESEARCH fresh / MEASURE byte-identical per v14.8 / → IDLE). Third frozen re-verify after run170; no EVOLVE churn (v15 holding). Did NOT: kickstart (E5 owner=jesse), ship, re-snapshot, or re-sweep.
