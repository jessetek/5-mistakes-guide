# 2026-07-13 — SEO Brain runs

## run48 — RESEARCH: post-March-2026-Core-Update refresh (RESULT: RESEARCHED)

**Why not IDLE (the frozen-tally reflex from runs 42–47):** knowledge `best-practices-2026.md`
was last fully researched **2026-06-11 = 32 days ago**, crossing the hourly prompt's "~30 days
stale ⇒ RESEARCH" trigger. Public/ is still frozen (`git rev-parse HEAD:public` = `77a6330` ==
`last-qc.json` anchor, tree clean ⇒ no whitelisted SAFE FIX pending). MEASURE still DARK
(`gsc-harvest-2026-06-01.json` = 42d stale, `weekly-rank-watch` absent from `launchctl` — E5,
owner=jesse, priority 0 ⇒ no new GSC data). So per the priority order: no SAFE FIX → **RESEARCH**.

**Tension noticed:** runs 42–47 all justified IDLE with "knowledge fresh (quarterly)", reading the
file's own `Refresh ~quarterly` line as the gate. But the operative gate is the hourly prompt's
**~30-day** RESEARCH trigger. Reconciled in SKILL 2 v13 (below) so future runs don't idle past 30d.

**Did (one-question SKILL-2 verify, cheap — 2 web searches + 1 fetch, not the 6-agent workflow):**
Verified "what on-page + schema signals now drive AI-Overview citations & local-pack support for
local real-estate/YMYL pages, and what changed recently?" → appended a dated, **7-source-cited**
refresh section to `knowledge/best-practices-2026.md` (+58 lines).

**Findings (all tied to the March-2026 Google Core Update, ~14-day rollout):**
- 43% of local packs now render an AI Overview above them.
- Review **recency + owner-response rate** overtook raw count (~2.3× recency-weight increase).
- GBP completeness now a stronger lever: incomplete ≈ 18% avg rank drop; photo completeness ≈ 67% impact.
- On-page is now the single largest LOCAL-ORGANIC factor (~19%; rises to ~24% / #1 for AI-Search
  visibility while GBP collapses ~32%→~12% in the AI-answer context).
- Entity-based local authority replaced pure keyword matching; hyper-local unique content rewarded,
  templated/national content suppressed.
- FAQPage/Article/Organization+Person schema that MIRRORS visible on-page Q&A boosts AI-Overview
  eligibility (schema must match visible copy, not exceed it).

**Backlog impact (SHARPENED, not reversed — the Jun-11 strategy holds):**
- C2 de-doorway is now the highest-value on-page fix (update actively suppresses ~476-word clones).
- C6 FAQ blocks = the concrete AI-Overview eligibility play, but JSON-LD must mirror visible Q&A.
- C3/C7 (Person author + entity sameAs/areaServed) feed entity authority, now above keyword-match.
- J2 review ask shifts volume→**recency + reply-to-all**; J1 GBP prioritize **photo/attribute completeness**.
- Ledger `updated`, `priority_order`, and C2/C6 `why` fields refreshed accordingly.

**Did NOT:** touch public/ — no new autonomous on-page action is unlocked (C2/C3/C6 all still need
Jesse's real local facts). Did not re-run the full 6-agent research workflow (a one-question verify
is the correct ~30d cadence; the full workflow is the ~quarterly re-run). Did not re-raise E5/E6.

RESULT: RESEARCHED

## Idle streak (post-run48, frozen-tally per SKILLS v11)

- **run49** — IDLE. public/ `77a6330` == `last-qc.json` anchor (QC clean since run41), tree clean ⇒ no SAFE FIX pending. Knowledge fresh (run48 refreshed it TODAY, 0d) ⇒ no RESEARCH. MEASURE dark (`gsc-harvest-2026-06-01.json`, 42d stale, `weekly-rank-watch` still absent — E5 owner=jesse) ⇒ no new data. All on-page backlog (C2/C3/C6) still gated on Jesse's real local facts; off-page (J1–J5) owner=jesse. E6 cost-throttle stays open, not re-raised. Tally line only, no public/ change.
