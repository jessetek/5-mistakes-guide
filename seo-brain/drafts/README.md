# seo-brain/drafts

Staging area for work the **hourly autonomous loop is not allowed to ship unattended** —
new marketing/city pages and anything that needs Jesse's real content (exact closings,
current market stats, client anecdotes, which cities he actively works).

The loop writes drafts here **instead of fabricating facts or shipping thin pages**.
Nothing in this folder is deployed. Review a draft, fill in the real facts, then move it
to `public/` (or tell Claude to finish + ship it).
